import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

// TODO problem with i18n instance
test('Renders without crashing TODO', async () => {
  // const { baseElement, findByText } = render(<App />);

  // expect(baseElement).toBeDefined();

  // await findByText("Nav.Map");
  // await findByText("Nav.Settings");
});
